// import { selectGoingToRead } from "../Auth/selectors";
// import { createSelector } from "@reduxjs/toolkit";

// export const selectBookId = (state) => state.filter.bookId;

// export const selectFilteredBookMemo = createSelector(
//   [selectGoingToRead, selectBookId],
//   (books, id) => {
//     return books.find((book) => book._id === id);
//   }
// );
